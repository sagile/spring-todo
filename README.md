# spring-todo

Setup DB:

```bash
 docker compose up -d
```

Run locally:

```bash
 ./gradlew bootRun
```

go to 
http://localhost:8080/register
http://localhost:8080/login
Optional:

```bash
 ./gradlew clean build
 ./gradlew build
 docker compose down 
```


